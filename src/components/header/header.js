import { Link } from "gatsby"
import PropTypes from "prop-types"
import React from "react"
import './header.scss';

const Header = () => (
  <header id="header">
    <div>
    </div>
  </header>
)

export default Header
